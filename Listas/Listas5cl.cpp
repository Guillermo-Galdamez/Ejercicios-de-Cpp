#include <iostream>
#include <list>
using namespace std;

struct Persona{
    string nombre;
    string apellido;
    int edad;
};
void menu(list<Persona>&list){
     
}
int main(){
    list<Persona> list;
    menu(list);
    return 0;
}